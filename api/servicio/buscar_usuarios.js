var express = require("express");
var router = express.Router();
var mongoose = require("mongoose");

var ParkingRequest = require("../modelo/modelo_usuario");

router.get("/buscar/persona", function (req, res) {});

module.exports = router;
